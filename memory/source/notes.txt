

GrowingFixedAllocator can grow. It can dynamically add new blocks of memory. This is appropriate in cases such as a prototyping library (when initially building a game). In a PC environment where memory can grow.


StagnentFixedAllocator once initialized this FixedAllocator can never ever grow.





